//export const Moveable = (self) => ({
//moveSprite: (vector) => {
//self.setVelocity(vector.x, vector.y);
//if (!self.isAttacking) this.play(`walk${this.orientation}`);
//},

//stopSprite: () => {
//self.setVelocity(0, 0);
//},
//});
